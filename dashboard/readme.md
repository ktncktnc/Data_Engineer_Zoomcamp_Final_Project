# Run below commands to set up Superset:

```
git clone https://github.com/apache/superset
cd superset
docker-compose -f docker-compose-non-dev.yml pull
docker-compose -f docker-compose-non-dev.yml up -d
```
